{
  "name": "mediadb",
  "version": "1.0.0",
  "description": "A media database API built with json-server for learning REST APIs and Postman testing.",
"scripts": {
  "start": "json-server --watch mediadb.json --port 3000 --delay 500 --static ./public",
  "dev": "json-server --watch mediadb.json --port 3000 --delay 500"
},
  "dependencies": {
    "json-server": "^0.17.4"
  }
}